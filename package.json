{
  "name": "level-accounts",
  "version": "1.0.0",
  "description": "Opinionated, simple and pretty secure token-based authentication solution using LevelDB",
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js; rm -rf test/db",
    "example": "node example/app.js",
    "postinstall": "npm star level-accounts &> /dev/null || exit 0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "level": "^0.18.0",
    "tape": "^3.4.0"
  },
  "dependencies": {
    "bcrypt": "^0.8.1",
    "bcrypt-nodejs": "0.0.3",
    "bcryptjs": "^2.1.0",
    "lodash": "^3.0.1",
    "node-uuid": "^1.4.2"
  }
}
